package exercise9.model.tiles;

import exercise9.model.TilePit;

public class BrokenBridgeTile extends TilePit
{
    public BrokenBridgeTile(final int column, final int row, final String tileFileName) {
        super(column, row, tileFileName);
    }
}